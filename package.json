{
  "name": "claimlens",
  "version": "0.1.0",
  "private": true,
  "description": "AI agent for patent claim analysis and infringement risk review.",
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace web",
    "build:web": "npm run build --workspace web",
    "lint:web": "npm run lint --workspace web"
  },
  "engines": {
    "node": ">=22",
    "npm": ">=10"
  },
  "optionalDependencies": {
    "lightningcss-linux-x64-gnu": "^1.32.0"
  }
}
